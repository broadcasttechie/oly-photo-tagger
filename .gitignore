*.iml
.gradle/
/local.properties
/.idea/
/.claude/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Android Studio generated files
*.apk
*.ap_
*.aab
*.dex
*.class

# Gradle
.gradle
build/

# Signing
*.jks
*.keystore
key.properties

# Log Files
*.log

# native/ build scratch — see native/README.md. out/ is produced by CI and
# copied into app/src/main/jniLibs + assets, not committed here directly.
/native/src/
/native/build/
/native/out/

# Real camera photos for local manual testing only — never commit actual photos to a
# repo headed for a public F-Droid release. See RealRawGpsWriteInstrumentedTest's class
# doc for how these get used.
/SamplePhotos/
